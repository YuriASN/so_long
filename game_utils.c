#include "so_long.h"

/*	If there's an item on new player position it collects it
	and open exit if there'es nothing else to be collected. */
void	is_component(t_prog *sol, int x, int y)
{
	int	i;

	i = -1;
}
